# MintStock — План внедрения

**Дата составления**: 22.02.2026  
**Статус**: Утверждён, готов к разработке  
**Модуль**: MintStock (управление складом и закупками)  
**Портал**: wtm.az/mintstock/  
**Компания**: Клининговая компания  
**Срок**: Март 2026 (⚠️ Срочно!)  
**Версия**: 1.8

---

## КОНТЕКСТ ПРОЕКТА

**Что это**: Единый модуль управления запасами клининговых материалов (моющие средства, перчатки, бумага, мопы, щётки и т.д.) и закупками для клининговой компании.

**Архитектура модуля**: Один модуль `MintStock` с внутренним разделением функций:
- **Warehouse** (Складские операции): приёмка, выдача, инвентаризация
- **Procurement** (Закупки): создание PO, управление поставщиками, отслеживание поставок
- **Shared** (Общее): товары, объекты, отчёты, уведомления

**Структура компании**:
- **Центральный склад (ЦС)** — главное хранилище всех материалов
- **Объекты (филиалы)** — места работы клининговых бригад, у каждого есть приёмная точка (мини-склад)

**Основной процесс**:
1. Супервайзор объекта создаёт запрос на пополнение материалов
2. Складовщик обрабатывает запрос и выдаёт товар (если есть на ЦС)
3. Если товара нет — ЦС даёт заказ Procurement → Procurement создаёт заказ (PO) поставщику
4. Товар приходит на ЦС → Складовщик принимает → выдаёт на объект
5. Периодическая инвентаризация на ЦС и объектах (по кнопке — можно начать в любой момент)

---

## СТАТУС ОБСУЖДЕНИЯ

| Блок | Статус |
|---|---|
| Роли пользователей | ✅ Согласовано |
| Базовые сущности | ✅ Согласовано |
| Бизнес-процессы | ✅ Согласовано |
| PO документ | ✅ Согласовано |
| PO статусы | ✅ Согласовано (v1.6) |
| Товары и категории | ✅ Согласовано (v1.6) |
| Уведомления | 🔶 Требует уточнения |
| Отчёты | 🔶 Требует уточнения |
| Технический стек | ✅ Согласовано |
| Интеграции | ✅ Согласовано |
| Архитектура модуля | ✅ Единый модуль с внутренним разделением |
| Инфраструктура (аудит) | ✅ Исправлена (v1.5) |

---

## ЧАСТЬ 1 — СОГЛАСОВАННЫЕ РЕШЕНИЯ

### 1.0 Архитектура модуля

**Решение: Единый модуль MintStock**

✅ **Преимущества:**
- Тесная интеграция склада и закупок (единый flow)
- Одна база данных, нет межсервисного взаимодействия
- Procurement видит остатки склада в реальном времени
- Быстрее разработка, проще поддержка
- Удобнее для пользователей (единая навигация)

**Внутреннее разделение:**
- Модуль `warehouse`: складские операции
- Модуль `procurement`: закупки и PO
- Модуль `shared`: общие сущности (товары, поставщики, объекты)

**Разграничение доступа через роли** (один модуль, разные права):
- Складовщик → только warehouse endpoints
- Procurement → только procurement endpoints  
- Operations Manager → все endpoints
- Admin → все endpoints

### 1.1 Общее

- **Название модуля**: MintStock
- **URL**: `wtm.az/mintstock/`
- **Язык интерфейса**: Единый с порталом (русский/азербайджанский)
- **Валюта**: Манат (₼)
- **Терминология**: "Объекты" (филиалы)

### 1.2 Роли пользователей

| Роль | Что делает | Маппинг MintAuth |
|---|---|---|
| **Администратор** | Полный доступ ко всем функциям: управление пользователями, объектами, товарами, поставщиками, лимитами, складские операции, закупки (PO) | `ADMIN` |
| **Operations Manager** | Полный доступ ко всем функциям склада и procurement: приём/выдача товара, создание PO, управление поставщиками | `OPERATIONS_MANAGER` |
| **Складовщик** | Приём товара на ЦС (с проверкой + фото), выдача товара по запросам объектов, инвентаризация ЦС | `WAREHOUSE_MANAGER` |
| **Procurement** (Закупщик) | Создание и управление заказами (PO), выбор поставщиков и переговоры, отслеживание поступлений | `PROCUREMENT` |
| **Супервайзор** (на объектах) | Формирование запросов на пополнение товаров для своих объектов, инвентаризация объектов | `SUPERVISOR` |

**Правила ролей:**
- Один человек = одна роль (совмещение недопустимо)
- **Супервайзор** привязывается к одному или нескольким объектам (видит только свои объекты)
- **Складовщик** и **Procurement** — все объекты (глобальный уровень)
- **Администратор** и **Operations Manager** — полный доступ ко всем функциям системы
- Пользователи создаются только через MintAuth

### 1.3 Технический стек

| Компонент | Решение | Обоснование |
|---|---|---|
| Backend | Node.js + Express + TypeScript | Единый с FeedbackQR/ATM |
| ORM | Prisma | Единый с FeedbackQR/ATM |
| База данных | PostgreSQL (новая БД `mintstock`) | Единый кластер |
| Frontend | React + Vite + TypeScript | Единый с другими модулями |
| Аутентификация | MintAuth JWT (`mint_session`) | SSO — уже работает |
| PDF генерация | `pdfkit` (Node.js) | Для PO документов |
| Загрузка файлов | `multer` | Фото при приёмке товара |
| Telegram | `node-telegram-bot-api` | Тот же бот что в FeedbackQR |
| Email | `nodemailer` (SMTP) | Единый с FeedbackQR |
| Excel/CSV export | `exceljs` + `csv-writer` | Для отчётов и импорта |
| Контейнеризация | Docker (multi-stage build) | Единый с другими модулями |
| Маршрутизация | Nginx (добавить upstream) | Единый портал |

### 1.4 Структура склада и выдачи

**Центральный склад (ЦС)**
- Хранилище всех клининговых материалов
- Управляется Складовщиком
- Место приёма закупленного товара
- Точка раздачи товара на объекты

**Объекты (филиалы)**
- Клининговые бригады, которые потребляют материалы
- При каждом объекте есть **приёмная точка** (мини-склад)
- Запросы поступают от Супервайзора объектов(так же оперейшен мененджер)
- Супервайзор видит: остатки товара на своих объектах (к которым привязан), историю поступлений
- Один Супервайзор может управлять несколькими объектами
- Складовщик выделяет товар из ЦС согласно запросу и отгружает на объект

### 1.5 Лимиты на объектах

- **Тип лимита**: По количеству (например: швабры на объекте "Баку Молл" → максимум 10 шт)
- **Кнопка "Авто-заполнить до лимита"**: 
  ```
  Лимит швабры = 10 шт
  Текущий остаток после инвентаризации = 3 шт
  → Кнопка подставит: запросить 7 шт (чтобы выйти на лимит 10)
  ```

### 1.6 PO документ (Purchase Order)

- **Язык**: Азербайджанский + Английский
- **Логотип**: Логотип компании клининга
- **Нумерация**: Да — формат `PO-2026-0001`
- **Шаблон**: Разрабатываем с нуля
- **Формат**: PDF

**Содержание PO:**
```
- Заголовок: "SİFARİŞ" / "PURCHASE ORDER"
- Номер PO и дата
- Данные отправителя (Mint Service, Центральный склад)
- Данные поставщика (название, контакт, телефон, email)
- Таблица товаров: наименование, кол-во, ед.изм., цена за ед., сумма
- Промежуточные итоги (если нужно)
- Итоговая сумма (TOTAL)
- Контактная информация для уточнений
- QR-код PO (для отслеживания если нужно)
- Нужная дата доставки
```

**Правила PO:**
- Один PO = один поставщик
- Разные поставщики → разные PO

### 1.7 Статусы PO (✅ согласовано v1.6)

```
DRAFT → SENT → RECEIVED → CLOSED
```

| Статус | Название RU | Описание |
|---|---|---|
| `DRAFT` | PO создана (черновик) | Procurement создал заказ, ещё не отправлен поставщику |
| `SENT` | PO отправлена поставщику | Отправлена по Email/Telegram, ожидаем поставку |
| `RECEIVED` | Товар на складе (принят) | Складовщик принял товар на ЦС, проверил и сфотографировал |
| `CLOSED` | PO закрыта | Финальный статус, все товары получены и распределены |

**Переходы:**
- `DRAFT` → `SENT`: Procurement нажимает "Отправить поставщику"
- `SENT` → `RECEIVED`: Складовщик нажимает "Принять товар" (с фото подтверждением)
- `RECEIVED` → `CLOSED`: Автоматически после распределения или вручную

### 1.8 Отправка PO поставщику

- **Telegram**: В личку поставщику (поставщик должен написать боту первым — получаем его chat_id)
- **Email**: Через SMTP (на email поставщика)
- **Уведомление** в систему (внутри портала) о том, что PO отправлена

### 1.9 Инвентаризация (✅ обновлено v1.6)

- Проводится **периодически** (не привязана к строгому расписанию)
- **По кнопке** — можно начать инвентаризацию в любой момент
- Один объект = одна активная инвентаризация (нельзя две параллельные)
- Проводит: Складовщик (ЦС) и Супервайзор (объект)
- Фиксируется фактическое количество → сравнивается с системным → отклонения

### 1.10 Складские операции

**Начальные остатки**:
- Будут загружены из Excel (однократно при запуске системы)

**Частичная выдача**:
- Да, Складовщик может выдать товар частично
- Пример: Объект запросил 10 швабр, на складе 6 → Складовщик выдаёт 6 сейчас, 4 — после закупки

**Приёмка товара**:
- Складовщик сам отмечает поступление
- Обязательно прикрепить документ (фото накладной или товара)
- Фото хранятся в Docker volume `mintstock_uploads`

### 1.11 Товары (✅ согласовано v1.6)

**Атрибуты товара:**
- **Наименование** — обязательное
- **Категория** — обязательная
- **Единица измерения** — обязательная (шт, л, кг, упак)

**Категории товаров:**
- Загружаются из Excel-файла (импорт при запуске)
- Админ может добавлять/редактировать категории через интерфейс

### 1.12 Поставщики

**Цена товара**:
- Цена у поставщика (разная для разных поставщиков)
- Пример: Швабра у Поставщика А = 5 ₼, у Поставщика Б = 4.5 ₼
- Procurement видит цены и выбирает выгоднее


**Telegram поставщика**:
- Поставщик должен написать боту чтобы получать PO в Telegram

---

## ЧАСТЬ 2 — УТОЧНЯЮЩИЕ ВОПРОСЫ ❓

> Оставшиеся вопросы для финальных уточнений.

### ❓ БЛОК В — Уведомления

**В1. Каналы и события**

По каждому событию — какой канал используется?

| События | Telegram | Email | Портал |
|---|---|---|---|
| Новый запрос на пополнение (для Складовщика) | ? | ? | ? |
| Запрос выполнен (для Супервайзора) | ? | ? | ? |
| PO создана (для информации) | ? | ? | ? |
| Товар поступил на ЦС | ? | ? | ? |
| Низкий остаток на ЦС | ? | ? | ? |
| Просроченная поставка | ? | ? | ? |

**В2. Telegram боты для сотрудников**

Для отправки уведомлений сотрудникам в Telegram — они тоже должны написать боту первым (чтобы получить chat_id)?

### ❓ БЛОК Г — Отчёты и аналитика

**Г1. Список отчётов**

| Отчёт | Кто смотрит | Детали |
|---|---|---|
| Остатки на ЦС (текущие) | Admin, Operations Manager, Procurement, Складовщик | По товарам, по категориям |
| История выдачи (ЦС → Объекты) | Все | За период, по объектам |
| История приёма (Поставщик → ЦС) | Admin, Operations Manager, Procurement, Складовщик | За период, по поставщикам |
| Расход по объекту | Супервайзор (свои объекты) + Admin + Operations Manager | За месяц, по товарам |
| PO и закупки | Admin, Operations Manager, Procurement | Стоимость, дата, статус |
| Изменение цены товара | Admin, Operations Manager, Procurement | История цен |
| Прогноз расхода | Admin, Operations Manager, Procurement | На основе истории |

**Г2. Экспорт**

Excel/CSV для всех отчётов? 

(Ответ: Да)

---

## ЧАСТЬ 3 — ПЛАН РАЗРАБОТКИ

**СРОК: Март 2026 (⚠️ Очень сжато!)**

Разбиение по неделям:
- **Неделя 1 (24-28 фев)**: Инфраструктура + начало Backend
- **Неделя 2-3 (3-14 мар)**: Backend + Frontend параллельно
- **Неделя 4 (17-21 мар)**: Интеграция + тестирование
- **Неделя 5 (24-28 мар)**: Деплой + go-live
- **Неделя 6 (31 мар)**: Fixes + стабилизация

### Фаза 0 — Инфраструктура и подготовка (24-28 февраля)

**Внешние интеграции (файлы вне MintStock):**
- [ ] `scripts/db/init-multiple-databases.sh` — добавить CREATE USER/DATABASE `mintstock`
- [ ] `MintAuth/backend/app/main.py` — добавить `ProjectInfo(name="MintStock", roles=[...])` в `get_projects()`
- [ ] `portal-index.html` — добавить карточку MintStock с `data-system="mintstock"`
- [ ] `portal.js` — добавить в `systemUrls` и `projectNameToSystem` maps
- [ ] `shared-components/ProjectSwitcher.tsx` — добавить MintStock в список проектов
- [ ] `docker-compose.all.yml` — добавить `mintstock-backend`, `mintstock-frontend` сервисы + volume `mintstock_uploads`
- [ ] `nginx-portal.conf` — добавить upstream + locations с `auth_request` паттерном

**Проектирование:**
- [ ] ER-диаграмма БД (сущности, связи)
- [ ] Макеты 10 основных экранов (Figma или Pencil)
- [ ] API контракты (OpenAPI/Swagger)
- [ ] Создать репо `/MintStock/backend/` и `/MintStock/frontend/`

**Данные:**
- [ ] Подготовить Excel с историческими данными (товары, категории, поставщики, остатки)

### Фаза 1 — Backend (3-14 марта)

**День 1-2 (3-4 мар):**
- [ ] Инициализация Node.js + Express + TypeScript проекта
- [ ] Настройка модульной структуры (modules/warehouse, modules/procurement, modules/shared)
- [ ] Prisma установка + подключение к `mintstock` БД
- [ ] ER-схема в Prisma (модели: Products, Suppliers, StockItems, Requests, PO, etc.)
- [ ] Миграции данных из Excel (товары, категории, остатки)

**День 3-5 (5-7 мар):**
- [ ] CRUD API: товары, объекты, поставщики, остатки (shared modules)
- [ ] API запросов на пополнение (warehouse module)
- [ ] API обработки на склад: выдача товара, приёмка товара (warehouse module)

**День 6-8 (10-12 мар):**
- [ ] API закупок + генерация PO в PDF (procurement module)
- [ ] Отправка PO по Email и Telegram (shared utils)
- [ ] Интеграция с MintAuth (JWT проверка, middleware ролей для warehouse/procurement)

**День 9-10 (13-14 мар):**
- [ ] Уведомления (события, каналы Telegram/Email)
- [ ] Отчёты (REST endpoints для скачивания Excel/CSV)
- [ ] API инвентаризации (создание, ввод данных, сверка, закрытие)
- [ ] Тестирование API (вручную + Postman)

### Фаза 2 — Frontend (3-14 марта, параллель с Backend)

**День 1-3 (3-5 мар):**
- [ ] React + Vite боилерплейт
- [ ] MintAuth SSO интеграция (Login -> JWT)
- [ ] Макет Дашборда (Dashboard)

**День 4-6 (6-8 мар):**
- [ ] Экран "Запрос на пополнение" (Супервайзор) с кнопкой "Авто-заполнить до лимита"
- [ ] Экран "Обработка запросов" (Складовщик)
- [ ] Экран "Приёмка товара" (Складовщик) с загрузкой фото

**День 7-9 (10-12 мар):**
- [ ] Экран "Управление товарами" (Admin) + импорт из Excel
- [ ] Экран "Управление объектами" (Admin)
- [ ] Экран "Создание PO" (Procurement) с 4-стадийным статусом

**День 10-11 (13-14 мар):**
- [ ] Экран "Инвентаризация" (кнопка "Начать инвентаризацию", ввод фактических остатков)
- [ ] Отчёты (таблицы, фильтры, export)
- [ ] Профиль, Навигация
- [ ] CSS, Responsive дизайн

### Фаза 3 — Интеграция в MintStudio (15-18 марта)

- [ ] Добавить в `docker-compose.all.yml`:
  - `mintstock-backend` (порт **3003**)
  - `mintstock-frontend` (multi-stage: Vite build → nginx :80)
  - Volume `mintstock_uploads` для фото приёмки
- [ ] Добавить routes в `nginx-portal.conf`:
  - Upstream `mintstock-backend:3003`
  - Location `/mintstock/api/` → backend с `auth_request`
  - Location `/mintstock/` → frontend
- [ ] Переменные окружения (`.env` для dev/prod)
- [ ] Карточка MintStock в главном дашборде портала
- [ ] Логирование, мониторинг (единые логи для всех модулей)
- [ ] Проверка разграничения доступа по ролям (middleware для warehouse/procurement)

### Фаза 4 — Тестирование (19-23 марта)

- [ ] Функциональное тестирование всех ролей
- [ ] Нагрузочное тестирование
- [ ] Проверка безопасности, авторизации
- [ ] Фиксы багов и дизайна
- [ ] Подготовка документации (README, API docs)

### Фаза 5 — Деплой и go-live (24-31 марта)

- [ ] Финальные данные в БД (начальные остатки, поставщики, лимиты)
- [ ] Деплой на продакшен
- [ ] Тестирование на живых данных
- [ ] Обучение пользователей
- [ ] 🚀 Live!

---

## ЧАСТЬ 4 — ЭКРАНЫ ФРОНТЕНДА

### Основные экраны (11 шт)

| # | Экран | URL | Роли | Назначение |
|---|---|---|---|---|
| 1 | **Авторизация** | (MintAuth) | Все | SSO через MintAuth |
| 2 | **Дашборд** | `/` | Все | Обзор по роли: запросы, остатки, уведомления |
| 3 | **Мои запросы** | `/requests` | Supervisor | Создание запроса на пополнение + кнопка "Авто-заполнить до лимита" |
| 4 | **Инвентаризация** | `/inventory` | Supervisor | Начать инвентаризацию, ввод остатков, расхождения |
| 5 | **Остатки склада** | `/stock` | Supervisor, Warehouse, OM, Admin | Текущие остатки (Supervisor — только свои объекты) |
| 6 | **Входящие запросы** | `/warehouse/requests` | Warehouse, OM, Admin | Просмотр, выдача (полная/частичная), создание PurchaseRequest |
| 7 | **Инвентаризация (склад)** | `/warehouse/inventory` | Warehouse, OM, Admin | Инвентаризация ЦС и объектов |
| 8 | **Объекты** | `/admin/locations` | Warehouse, OM, Admin | CRUD объектов, лимиты, привязка супервайзоров |
| 9 | **Запросы на закупку** | `/procurement/purchase-requests` | Procurement, OM, Admin | Список от складовщика, создание PO |
| 10 | **Purchase Orders** | `/procurement/orders` | Procurement, OM, Admin | Создание PO, PDF, отправка Email/Telegram, приёмка |
| 11 | **Поставщики** | `/procurement/suppliers` | Procurement, OM, Admin | CRUD поставщиков, цены по товарам |
| 12 | **Товары** | `/admin/products` | Procurement, OM, Admin | CRUD товаров, категории, импорт из Excel |
| 13 | **Отчёты** | `/reports` | Все (с фильтрами по ролям) | Остатки, расход, закупки, история — Excel экспорт |

### Навигация sidebar по ролям

| Пункт меню | SUPERVISOR | WAREHOUSE | PROCUREMENT | OM / ADMIN |
|---|:---:|:---:|:---:|:---:|
| 📊 Дашборд (`/`) | ✅ | ✅ | ✅ | ✅ |
| 📋 Мои запросы (`/requests`) | ✅ | — | — | — |
| 📦 Инвентаризация (`/inventory`) | ✅ | — | — | — |
| 📊 Остатки склада (`/stock`) | ✅ | ✅ | — | ✅ |
| 📥 Входящие запросы (`/warehouse/requests`) | — | ✅ | — | ✅ |
| 📦 Инвентаризация (`/warehouse/inventory`) | — | ✅ | — | ✅ |
| 🏢 Объекты (`/admin/locations`) | — | ✅ | — | ✅ |
| 🛒 Запросы на закупку (`/procurement/purchase-requests`) | — | — | ✅ | ✅ |
| 📄 Purchase Orders (`/procurement/orders`) | — | — | ✅ | ✅ |
| 🤝 Поставщики (`/procurement/suppliers`) | — | — | ✅ | ✅ |
| 🛍️ Товары (`/admin/products`) | — | — | ✅ | ✅ |
| 📈 Отчёты (`/reports`) | ✅ | ✅ | ✅ | ✅ |

### Доп. экраны (при времени)

| # | Экран | Роли |
|---|---|---|
| 14 | История (Log) изменений | Admin |

---

## ЧАСТЬ 5 — АРХИТЕКТУРА

### 5.1 Порты и сетевая архитектура

| Сервис | Порт | Примечание |
|---|---|---|
| MintAuth (Python/FastAPI) | **8000** | SSO, управление ролями |
| FeedbackQR | **3001** | Уже занят |
| FeedbackATM | **3002** | Уже занят |
| **MintStock Backend** | **3003** | ← Новый |
| MintStock Frontend | **80** (внутри контейнера) | Multi-stage build: Vite → nginx |

### 5.2 Структура файлов (модульная архитектура)

```
/MintStock/
├── backend/
│   ├── src/
│   │   ├── modules/                # Функциональные модули
│   │   │   ├── warehouse/          # Складские операции
│   │   │   │   ├── controllers/    # Приёмка, выдача, инвентаризация
│   │   │   │   ├── services/
│   │   │   │   └── routes/
│   │   │   ├── procurement/        # Закупки
│   │   │   │   ├── controllers/    # PO, поставщики, заказы
│   │   │   │   ├── services/
│   │   │   │   └── routes/
│   │   │   ├── products/           # Товары (общее)
│   │   │   │   ├── controllers/
│   │   │   │   ├── services/
│   │   │   │   └── routes/
│   │   │   ├── locations/          # Объекты (общее)
│   │   │   └── reports/            # Отчёты (общее)
│   │   ├── shared/                 # Общая логика
│   │   │   ├── middleware/         # Auth, роли, валидация
│   │   │   ├── utils/              # PDF, Email, Telegram, multer
│   │   │   └── types/
│   │   └── index.ts                # Entry point (порт 3003)
│   ├── uploads/                    # Фото приёмки (mounted volume)
│   ├── prisma/
│   │   ├── schema.prisma           # Единая схема БД
│   │   └── migrations/
│   ├── package.json
│   ├── .env.template
│   ├── Dockerfile
│   └── README.md
├── frontend/
│   ├── src/
│   │   ├── components/             # Reusable UI
│   │   ├── pages/
│   │   │   ├── warehouse/          # Страницы склада
│   │   │   ├── procurement/        # Страницы закупок
│   │   │   ├── products/           # Управление товарами
│   │   │   ├── locations/          # Управление объектами
│   │   │   ├── inventory/          # Инвентаризация
│   │   │   └── reports/            # Отчёты
│   │   ├── hooks/
│   │   ├── services/               # API clients
│   │   ├── utils/
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── vite.config.ts
│   ├── package.json
│   ├── Dockerfile                  # Multi-stage: node build → nginx
│   └── README.md
├── PLAN.md
└── README.md
```

### 5.3 API Routes

**Health check:**
- `GET /api/health` — проверка работоспособности backend

**Warehouse module:**
- `POST /api/warehouse/receive` — Приёмка товара (+ фото через multer)
- `POST /api/warehouse/issue` — Выдача товара
- `GET /api/warehouse/requests` — Список запросов
- `POST /api/warehouse/requests` — Создать запрос (Супервайзор)
- `PATCH /api/warehouse/requests/:id` — Обновить статус запроса
- `POST /api/warehouse/inventory` — Начать инвентаризацию
- `PUT /api/warehouse/inventory/:id` — Обновить данные инвентаризации
- `POST /api/warehouse/inventory/:id/close` — Закрыть инвентаризацию

**Procurement module:**
- `GET /api/procurement/purchase-requests` — Список запросов на закупку (от Складовщика)
- `POST /api/warehouse/purchase-requests` — Создать запрос на закупку (Складовщик → Закупщик)
- `PATCH /api/procurement/purchase-requests/:id` — Обновить статус (IN_PROGRESS/DONE)
- `POST /api/procurement/po` — Создать PO (статус DRAFT, из одного или нескольких PurchaseRequest)
- `GET /api/procurement/po` — Список PO
- `GET /api/procurement/po/:id` — Получить PO
- `POST /api/procurement/po/:id/send` — Отправить PO поставщику (DRAFT → SENT)
- `POST /api/procurement/po/:id/receive` — Принять товар по PO (SENT → RECEIVED)
- `POST /api/procurement/po/:id/close` — Закрыть PO (RECEIVED → CLOSED)
- `GET /api/procurement/po/:id/pdf` — Скачать PDF
- `GET /api/procurement/suppliers` — Список поставщиков
- `POST /api/procurement/suppliers` — Добавить поставщика

**Shared endpoints:**
- `GET /api/products` — Товары
- `POST /api/products` — Создать товар
- `POST /api/products/import` — Импорт товаров из Excel
- `GET /api/categories` — Категории
- `POST /api/categories/import` — Импорт категорий из Excel
- `GET /api/locations` — Объекты
- `GET /api/stock` — Остатки (с фильтрами по location)
- `GET /api/reports/*` — Отчёты (с export в Excel/CSV)

### 5.4 Основные сущности БД (Prisma)

```prisma
model Product {
  id          String   @id @default(cuid())
  name        String
  categoryId  String
  category    Category @relation(fields: [categoryId], references: [id])
  unit        String   // шт, л, кг, упак
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  stockItems            StockItem[]
  requestItems          RequestItem[]
  purchaseRequestItems  PurchaseRequestItem[]
  poItems               PurchaseOrderItem[]
  supplierPrices        SupplierPrice[]
}

model Category {
  id        String    @id @default(cuid())
  name      String    @unique
  createdAt DateTime  @default(now())
  products  Product[]
}

model Supplier {
  id          String   @id @default(cuid())
  name        String
  contact     String
  phone       String
  email       String
  telegramId  String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  purchaseOrders PurchaseOrder[]
  supplierPrices SupplierPrice[]
}

model SupplierPrice {
  id          String   @id @default(cuid())
  supplierId  String
  productId   String
  price       Float    // Цена в манатах (₼)
  updatedAt   DateTime @updatedAt

  supplier    Supplier @relation(fields: [supplierId], references: [id])
  product     Product  @relation(fields: [productId], references: [id])

  @@unique([supplierId, productId])
}

model Location {
  id          String   @id @default(cuid())
  name        String
  type        String   // WAREHOUSE | SITE
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  stockItems          StockItem[]
  requests            Request[]
  supervisorLocations SupervisorLocation[]
  inventories         Inventory[]
}

model SupervisorLocation {
  id            String   @id @default(cuid())
  supervisorId  String   // User ID из MintAuth (роль SUPERVISOR)
  locationId    String
  assignedAt    DateTime @default(now())

  location      Location @relation(fields: [locationId], references: [id])

  @@unique([supervisorId, locationId])
}

model StockItem {
  id          String   @id @default(cuid())
  productId   String
  locationId  String
  quantity    Int      @default(0)
  limitQty    Int?     // Лимит по количеству (только для SITE)
  updatedAt   DateTime @updatedAt

  product     Product  @relation(fields: [productId], references: [id])
  location    Location @relation(fields: [locationId], references: [id])

  @@unique([productId, locationId])
}

model Request {
  id          String        @id @default(cuid())
  locationId  String
  status      RequestStatus @default(PENDING)
  createdBy   String        // User ID из MintAuth
  note        String?
  createdAt   DateTime      @default(now())
  updatedAt   DateTime      @updatedAt

  location    Location      @relation(fields: [locationId], references: [id])
  items       RequestItem[]
  issueRecords IssueRecord[]
}

model RequestItem {
  id          String  @id @default(cuid())
  requestId   String
  productId   String
  quantity    Int     // Запрошенное кол-во
  issued      Int     @default(0) // Выданное кол-во (для частичной выдачи)

  request     Request @relation(fields: [requestId], references: [id])
  product     Product @relation(fields: [productId], references: [id])
}

enum RequestStatus {
  PENDING    // Ожидает обработки
  APPROVED   // Одобрен
  PARTIAL    // Частично выдан
  FULFILLED  // Полностью выдан
  REJECTED   // Отклонён
}

// Запрос на закупку: Складовщик → Закупщик
// Создаётся когда на ЦС не хватает товара для выполнения Request
model PurchaseRequest {
  id          String               @id @default(cuid())
  status      PurchaseRequestStatus @default(PENDING)
  createdBy   String               // User ID (Складовщик)
  note        String?
  createdAt   DateTime             @default(now())
  updatedAt   DateTime             @updatedAt

  items       PurchaseRequestItem[]
  po          PurchaseOrder?        // Один PO создаётся по одному или нескольким PurchaseRequest
}

enum PurchaseRequestStatus {
  PENDING    // Ожидает обработки Закупщиком
  IN_PROGRESS // Закупщик создаёт PO
  DONE       // PO создана
}

model PurchaseRequestItem {
  id                String          @id @default(cuid())
  purchaseRequestId String
  productId         String
  quantity          Int             // Сколько нужно докупить

  purchaseRequest   PurchaseRequest @relation(fields: [purchaseRequestId], references: [id])
  product           Product         @relation(fields: [productId], references: [id])
}

model PurchaseOrder {
  id          String   @id @default(cuid())
  poNumber    String   @unique  // PO-2026-0001
  supplierId  String
  status      POStatus @default(DRAFT)
  totalAmount Float    @default(0)
  createdBy   String   // User ID из MintAuth
  createdAt   DateTime @default(now())
  sentAt      DateTime?
  receivedAt  DateTime?
  closedAt    DateTime?

  supplier    Supplier            @relation(fields: [supplierId], references: [id])
  items       PurchaseOrderItem[]
  receiveRecords ReceiveRecord[]
}

enum POStatus {
  DRAFT     // PO создана (черновик)
  SENT      // PO отправлена поставщику
  RECEIVED  // Товар на складе (принят)
  CLOSED    // PO закрыта
}

model PurchaseOrderItem {
  id            String  @id @default(cuid())
  poId          String
  productId     String
  quantity      Int           // Заказано
  receivedQty   Int     @default(0) // Реально принято (для частичных поставок)
  unitPrice     Float         // Цена за единицу (₼)
  totalPrice    Float         // quantity * unitPrice

  po            PurchaseOrder @relation(fields: [poId], references: [id])
  product       Product       @relation(fields: [productId], references: [id])
}

model ReceiveRecord {
  id          String   @id @default(cuid())
  poId        String
  receivedBy  String   // User ID (Складовщик)
  photoUrl    String?  // Фото накладной/товара
  note        String?
  receivedAt  DateTime @default(now())

  po          PurchaseOrder @relation(fields: [poId], references: [id])
}

model IssueRecord {
  id          String   @id @default(cuid())
  requestId   String
  productId   String
  quantity    Int      // Выданное кол-во
  issuedBy    String   // User ID (Складовщик)
  issuedAt    DateTime @default(now())

  request     Request  @relation(fields: [requestId], references: [id])
}

model Inventory {
  id          String          @id @default(cuid())
  locationId  String
  status      InventoryStatus @default(IN_PROGRESS)
  conductedBy String          // User ID
  startedAt   DateTime        @default(now())
  closedAt    DateTime?

  location    Location        @relation(fields: [locationId], references: [id])
  items       InventoryItem[]
}

enum InventoryStatus {
  IN_PROGRESS
  COMPLETED
}

model InventoryItem {
  id            String    @id @default(cuid())
  inventoryId   String
  productId     String
  systemQty     Int       // Кол-во в системе
  actualQty     Int       // Фактическое кол-во
  difference    Int       // actualQty - systemQty

  inventory     Inventory @relation(fields: [inventoryId], references: [id])
}
```

### 5.5 Docker конфигурация

**Backend Dockerfile:**
```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY prisma ./prisma/
RUN npx prisma generate
COPY dist ./dist/
EXPOSE 3003
CMD ["node", "dist/index.js"]
```

**Frontend Dockerfile (multi-stage):**
```dockerfile
# Stage 1: Build
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# Stage 2: Serve via nginx
FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```

**docker-compose.all.yml (новые сервисы):**
```yaml
  mintstock-backend:
    build: ./MintStock/backend
    container_name: mintstock-backend
    restart: unless-stopped
    # Нет внешних портов — только через Nginx (как FeedbackQR/ATM)
    environment:
      - DATABASE_URL=postgresql://mintstock:${MINTSTOCK_DB_PASSWORD}@postgres:5432/mintstock
      - MINTAUTH_URL=http://mintauth-backend:8000
      - JWT_SECRET=${JWT_SECRET}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - UPLOAD_DIR=/app/uploads
    volumes:
      - mintstock_uploads:/app/uploads
    depends_on:
      - postgres
    networks:
      - mint-network

  mintstock-frontend:
    build: ./MintStock/frontend
    container_name: mintstock-frontend
    restart: unless-stopped
    networks:
      - mint-network

# В секции volumes:
volumes:
  mintstock_uploads:
```

**nginx-portal.conf (добавить):**
```nginx
upstream mintstock-backend {
    server mintstock-backend:3003;
}

upstream mintstock-frontend {
    server mintstock-frontend:80;
}

# MintStock API (защищённый)
location /mintstock/api/ {
    auth_request /mintauth/auth/verify;
    auth_request_set $auth_user $upstream_http_x_user_id;
    proxy_set_header X-User-Id $auth_user;
    proxy_pass http://mintstock-backend/api/;
}

# MintStock Frontend
location /mintstock/ {
    auth_request /mintauth/auth/verify;
    proxy_pass http://mintstock-frontend/;
}

# MintStock uploads (фото приёмки) — через backend, не alias
location /mintstock/uploads/ {
    auth_request /mintauth/auth/verify;
    rewrite ^/mintstock/uploads/(.*)$ /uploads/$1 break;
    proxy_pass http://mintstock-backend;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Cookie $http_cookie;
}
```

**scripts/db/init-multiple-databases.sh (добавить):**
```bash
# MintStock
psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" <<-EOSQL
    CREATE USER mintstock WITH PASSWORD '$MINTSTOCK_DB_PASSWORD';
    CREATE DATABASE mintstock OWNER mintstock;
    GRANT ALL PRIVILEGES ON DATABASE mintstock TO mintstock;
EOSQL
```

**MintAuth registration (main.py → get_projects):**
```python
ProjectInfo(
    name="MintStock",
    roles=["WAREHOUSE_MANAGER", "PROCUREMENT", "OPERATIONS_MANAGER", "SUPERVISOR"]
)
```

### 5.6 ENV переменные

```env
# MintStock Backend
DATABASE_URL=postgresql://mintstock:PASSWORD@postgres:5432/mintstock
MINTAUTH_URL=http://mintauth-backend:8000
JWT_SECRET=same_as_other_services
PORT=3003

# Telegram
TELEGRAM_BOT_TOKEN=same_bot_as_feedbackqr

# SMTP
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@example.com
SMTP_PASS=password

# Uploads
UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=10485760  # 10MB

# DB init
MINTSTOCK_DB_PASSWORD=secure_password
```

---

## ЧАСТЬ 6 — РИСКИ И ЗАВИСИМОСТИ

### Потенциальные риски ⚠️

| Риск | Тяжесть | Способ смягчения |
|---|---|---|
| Очень сжатые сроки (март) | 🔴 Высокая | Параллельная разработка Backend + Frontend, готовые шаблоны |
| Миграция исторических данных | 🟡 Средняя | Подготовить Excel заранее, написать скрипт загрузки |
| Роли в MintAuth не готовы | 🟡 Средняя | Проверить или создать роли заранее |
| Excel с категориями не готов | 🟡 Средняя | Запросить у заказчика заранее |
| Telegram бот limitations | 🟢 Низкая | Расширить текущий или создать новый бот |
| Интеграция с Nginx | 🟢 Низкая | Тестировать на staging перед production |

### External зависимости (должны быть готовы)

- ✅ **MintAuth** (для SSO и ролей) — порт 8000
- ✅ **PostgreSQL кластер** (для БД `mintstock`)
- ✅ **Telegram bot** (может быть тот же что в FeedbackQR)
- ✅ **SMTP сервер** (для Email)
- ✅ **Docker инфра** (готовая в MintStudio)
- ⏳ **Excel с товарами и категориями** (нужен от заказчика)

---

## ЧАСТЬ 7 — ЧЕК-ЛИСТ ПЕРЕД НАЧАЛОМ

### 📋 Подготовка данных

- [ ] Excel: товары (наименование, категория, ед.изм.)
- [ ] Excel: категории товаров
- [ ] Excel: поставщики (название, контакт, телефон, email, адрес)
- [ ] Excel: исторические остатки (ЦС и объекты, по состоянию на дату)
- [ ] Список объектов (филиалов) с ответственными Супервайзорами
- [ ] Логотип компании (PNG, 512x512 минимум)

### 🔧 Инфраструктура

- [ ] PostgreSQL: создать БД `mintstock` через `init-multiple-databases.sh`
- [ ] MintAuth: зарегистрировать проект MintStock с ролями `WAREHOUSE_MANAGER`, `PROCUREMENT`, `OPERATIONS_MANAGER`, `SUPERVISOR`
- [ ] Nginx: добавить upstream `mintstock-backend:3003` и `mintstock-frontend:80`
- [ ] Nginx: добавить location `/mintstock/` с `auth_request` паттерном
- [ ] Docker: подготовить Dockerfile для backend (порт 3003) и frontend (multi-stage → nginx :80)
- [ ] Docker: обновить `docker-compose.all.yml` с новыми сервисами + volume `mintstock_uploads`
- [ ] Портал: добавить карточку MintStock в `portal-index.html` и `portal.js`
- [ ] ProjectSwitcher: добавить MintStock в `shared-components/ProjectSwitcher.tsx`
- [ ] `.env`: добавить `MINTSTOCK_DB_PASSWORD`
- [ ] Git: создать структуру `/MintStock/backend/` и `/MintStock/frontend/`

---

## СЛЕДУЮЩИЕ ШАГИ

**Ближайшие действия:**
1. ✅ ~~Согласовать PO статусы~~ (DRAFT → SENT → RECEIVED → CLOSED)
2. ✅ ~~Согласовать атрибуты товаров~~ (наименование, категория, ед.изм.)
3. 🔶 **Уточнить уведомления** (БЛОК В)
4. 🔶 **Уточнить отчёты** (БЛОК Г)
5. ⏳ **Получить Excel** с товарами, категориями, поставщиками
6. ⏳ **Создать инфраструктуру** (Фаза 0)
7. ⏳ **Старт разработки**: после утверждения

---

## ИСТОРИЯ ИЗМЕНЕНИЙ

| Дата | Версия | Изменение |
|---|---|---|
| 22.02.2026 | 1.0 | Создан документ, первичные согласованные решения |
| 22.02.2026 | 1.1 | Уточнение контекста: клининговая компания, ЦС + объекты, роли (Складовщик + Procurement), срок март 2026 |
| 22.02.2026 | 1.2 | Развёрнутый план разработки (5 фаз), уточняющие вопросы, техстек, риски, чеклист, архитектура |
| 22.02.2026 | 1.3 | Уточнение ролей: Супервайзор → несколько объектов, Operations Manager с полным доступом |
| 22.02.2026 | 1.4 | Единый модуль MintStock с внутренним разделением warehouse/procurement/shared |
| 22.02.2026 | 1.5 | **Аудит инфраструктуры**: порт 3003 (не 3000), MINTAUTH_URL → mintauth-backend:8000, frontend multi-stage build, DB init script, portal integration, upload volume, health check |
| 22.02.2026 | 1.6 | **Ответы на вопросы**: PO статусы (DRAFT→SENT→RECEIVED→CLOSED), атрибуты товаров (наименование, категория, ед.изм.), категории из Excel, инвентаризация по кнопке (не строго по расписанию), расширенный основной процесс, экран инвентаризации в основные, Prisma schema обновлена (Category model, Inventory model, RequestItem, enums) |
| 23.02.2026 | 1.8 | **Навигация**: уточнена матрица видимости меню по ролям, добавлена таблица sidebar, обновлён список экранов (13 основных + 1 доп), маршрут Поставщиков перенесён в `/procurement/suppliers` |
| 22.02.2026 | 1.7 | **Аудит и правки**: (1) Логотип подтверждён; (2) Добавлена модель `PurchaseRequest` + `PurchaseRequestItem` — промежуточный шаг Складовщик→Закупщик; (3) Лимит = `limitQty Int?` (только по количеству); (4) `PurchaseOrderItem.receivedQty` для частичных поставок; (5) Nginx uploads исправлен на `proxy_pass` вместо `alias`; (6) Docker-compose: убран внешний порт 3003 (только через Nginx как у FeedbackQR/ATM); (7) API routes дополнены purchase-requests endpoints |

---

**Статус документа**: ✅ Утверждён, готов к разработке  
**Последнее обновление**: 22.02.2026  
**Версия**: 1.8
